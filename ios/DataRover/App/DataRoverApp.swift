import SwiftUI

@main
struct DataRoverApp: App {
    var body: some Scene {
        WindowGroup {
            // Task 2 owns the real shell (framebuffer view + touch + toolbar).
            // Skeleton placeholder so the app target builds and links the lib.
            Text("DataRover")
        }
    }
}
